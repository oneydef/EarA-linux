## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] `python3 -m unittest discover -s tests -v` passes locally
- [ ] i18n: new/changed UI strings updated in **both** `en` and `uk` in `eara/i18n.py`
- [ ] Tested on a paired Nothing / CMF device when the change touches connect, protocol, or GUI (note model + distro below)
- [ ] Focused diff — matches existing style in `eara/`

## Device testing (if applicable)

| Field | Value |
|-------|-------|
| Model | <!-- e.g. Nothing Ear (a) --> |
| Distro | <!-- e.g. Ubuntu 24.04, PipeWire --> |
| What was verified | <!-- connect / battery / ANC / EQ / gestures / fit / A2DP --> |

## Notes

<!-- Optional: `eara status --json` snippet, screenshots, follow-ups. -->
